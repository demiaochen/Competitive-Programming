// #include <bits/stdc++.h>
#include <iostream>
#include <cstdio>
#include <algorithm> // fill(first, last, value)
#include <vector>    // vec.assign(n, -1);
#include <string>
#include <cstring>  // memset()
#include <climits>  // INT_MAX  INT_MIN
#include <utility>  // swap(), make_pair()
#include <cmath>    // pow()   2^n == 1<<n  x<<k x*2^k  1LL << k  1ULL << k
#include <set>
#include <map>
#include <queue>

#ifdef DEBUG
    #define D(x...) printf(x)
#else  
    #define D(x...)
#endif

// #define int int64_t
using namespace std;

using ll = long long;        //typedef long long    ll;  
using ld = long double;      //typedef long double  ld;  
using ull = unsigned long long;
typedef pair<ll, ll> pii;
#define vt vector
#define ALL(v) v.begin(), v.end()
#define SORT(v) sort(ALL(v))
#define MEM(a,b) memset(a,b,sizeof a)

#define REP(n) for (int i = 0; i < n; ++i)          // repeat n times, i as index
#define REPP(i, n) for (int i = 0; i < n; ++i)      // repeat n times, arg0 as index
#define FO(m, n) for (int i = m; i <= n; ++i)       // from m to n, ++i
#define FOV(m, n) for (int i = m; i >= n; --i)      // from m to n, --i
#define FOR(i, a, b) for (int i = a; i <= b; ++i)   
#define FORV(i, a, b) for (int i = a; i >= b; --i)

#define INF 0x3f3f3f3f  // 1e9
#define INFF 0x3f3f3f3f3f3f3f3f  // 1e18
#define fastIO() ios_base::sync_with_stdio(false); cin.tie(nullptr); 
#define cout(a) std::cout << a << "\n";
#define coutt(a) std::cout << a << " ";

#define MAXN 1000005 // 1e6 4MB
const int N = 1e5 + 10;
const int mod = 1e9 + 7;


signed main() {
    fastIO();

}       
